<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>UYL — Up Your Language</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Sans:ital,wght@0,300;0,400;0,500;1,300&display=swap" rel="stylesheet"/>
<style>
  :root {
    --bg: #0a0a0f;
    --surface: #12121a;
    --surface2: #1a1a26;
    --accent: #f0c040;
    --accent2: #ff6b6b;
    --accent3: #4ecdc4;
    --text: #f0efe8;
    --muted: #7a7a8c;
    --border: rgba(240,192,64,0.15);
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--bg);
    color: var(--text);
    overflow-x: hidden;
  }

  /* ── NOISE OVERLAY ── */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 1000;
    opacity: 0.4;
  }

  /* ── NAV ── */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.2rem 3rem;
    background: rgba(10,10,15,0.85);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--border);
  }

  .logo {
    font-family: 'Syne', sans-serif;
    font-weight: 800;
    font-size: 1.5rem;
    letter-spacing: -0.02em;
  }

  .logo span { color: var(--accent); }

  .nav-links {
    display: flex;
    gap: 2.5rem;
    list-style: none;
  }

  .nav-links a {
    text-decoration: none;
    color: var(--muted);
    font-size: 0.9rem;
    font-weight: 500;
    transition: color 0.2s;
    letter-spacing: 0.03em;
  }

  .nav-links a:hover { color: var(--text); }

  .nav-cta {
    background: var(--accent);
    color: var(--bg) !important;
    padding: 0.55rem 1.4rem;
    border-radius: 100px;
    font-weight: 600 !important;
    transition: transform 0.2s, box-shadow 0.2s !important;
  }

  .nav-cta:hover {
    transform: translateY(-1px);
    box-shadow: 0 4px 20px rgba(240,192,64,0.35) !important;
    color: var(--bg) !important;
  }

  /* ── HERO ── */
  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 8rem 2rem 4rem;
    position: relative;
    overflow: hidden;
  }

  .hero-glow {
    position: absolute;
    width: 700px;
    height: 700px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(240,192,64,0.12) 0%, transparent 70%);
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    pointer-events: none;
    animation: pulse 4s ease-in-out infinite;
  }

  .hero-glow2 {
    position: absolute;
    width: 400px;
    height: 400px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(78,205,196,0.08) 0%, transparent 70%);
    top: 30%;
    left: 20%;
    pointer-events: none;
    animation: pulse 6s ease-in-out infinite reverse;
  }

  @keyframes pulse {
    0%, 100% { transform: translate(-50%, -50%) scale(1); opacity: 1; }
    50% { transform: translate(-50%, -50%) scale(1.1); opacity: 0.7; }
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(240,192,64,0.1);
    border: 1px solid rgba(240,192,64,0.3);
    color: var(--accent);
    padding: 0.4rem 1rem;
    border-radius: 100px;
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    margin-bottom: 2rem;
    animation: fadeUp 0.8s ease both;
  }

  .hero h1 {
    font-family: 'Syne', sans-serif;
    font-size: clamp(3.5rem, 9vw, 7.5rem);
    font-weight: 800;
    line-height: 0.95;
    letter-spacing: -0.04em;
    margin-bottom: 1.5rem;
    animation: fadeUp 0.8s 0.1s ease both;
  }

  .hero h1 .line2 {
    display: block;
    -webkit-text-stroke: 1.5px var(--accent);
    color: transparent;
  }

  .hero h1 .accent-word { color: var(--accent); }

  .hero p {
    max-width: 520px;
    color: var(--muted);
    font-size: 1.15rem;
    line-height: 1.7;
    margin-bottom: 2.5rem;
    animation: fadeUp 0.8s 0.2s ease both;
  }

  .hero-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    animation: fadeUp 0.8s 0.3s ease both;
  }

  .btn-primary {
    background: var(--accent);
    color: var(--bg);
    border: none;
    padding: 0.9rem 2.2rem;
    border-radius: 100px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
    font-family: 'DM Sans', sans-serif;
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(240,192,64,0.4);
  }

  .btn-secondary {
    background: transparent;
    color: var(--text);
    border: 1px solid rgba(255,255,255,0.15);
    padding: 0.9rem 2.2rem;
    border-radius: 100px;
    font-size: 1rem;
    font-weight: 500;
    cursor: pointer;
    transition: border-color 0.2s, background 0.2s;
    font-family: 'DM Sans', sans-serif;
  }

  .btn-secondary:hover {
    border-color: rgba(255,255,255,0.35);
    background: rgba(255,255,255,0.05);
  }

  /* Floating language bubbles */
  .lang-bubbles {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
  }

  .bubble {
    position: absolute;
    font-size: 1.4rem;
    font-family: 'Syne', sans-serif;
    font-weight: 700;
    opacity: 0.12;
    animation: float linear infinite;
  }

  .bubble:nth-child(1)  { left: 5%;  top: 20%; font-size: 2rem; animation-duration: 18s; animation-delay: 0s; }
  .bubble:nth-child(2)  { left: 88%; top: 15%; font-size: 1.2rem; animation-duration: 22s; animation-delay: -4s; }
  .bubble:nth-child(3)  { left: 15%; top: 70%; font-size: 1.8rem; animation-duration: 16s; animation-delay: -8s; }
  .bubble:nth-child(4)  { left: 80%; top: 65%; font-size: 2.2rem; animation-duration: 20s; animation-delay: -2s; }
  .bubble:nth-child(5)  { left: 50%; top: 10%; font-size: 1rem;  animation-duration: 25s; animation-delay: -12s; }
  .bubble:nth-child(6)  { left: 70%; top: 40%; font-size: 1.5rem; animation-duration: 19s; animation-delay: -6s; }
  .bubble:nth-child(7)  { left: 3%;  top: 45%; font-size: 1.1rem; animation-duration: 23s; animation-delay: -10s; }
  .bubble:nth-child(8)  { left: 92%; top: 80%; font-size: 1.6rem; animation-duration: 17s; animation-delay: -3s; }

  @keyframes float {
    0%   { transform: translateY(0) rotate(0deg); }
    50%  { transform: translateY(-30px) rotate(5deg); }
    100% { transform: translateY(0) rotate(0deg); }
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  /* ── STATS STRIP ── */
  .stats {
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 0;
  }

  .stat-item {
    padding: 2rem 4rem;
    text-align: center;
    border-right: 1px solid var(--border);
  }

  .stat-item:last-child { border-right: none; }

  .stat-number {
    font-family: 'Syne', sans-serif;
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--accent);
    line-height: 1;
    margin-bottom: 0.3rem;
  }

  .stat-label {
    color: var(--muted);
    font-size: 0.85rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  /* ── SECTION ── */
  section {
    padding: 7rem 2rem;
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
  }

  .section-tag {
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .section-tag::before {
    content: '';
    display: block;
    width: 20px;
    height: 2px;
    background: var(--accent);
  }

  .section-title {
    font-family: 'Syne', sans-serif;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 800;
    letter-spacing: -0.03em;
    line-height: 1.1;
    margin-bottom: 1.2rem;
  }

  .section-subtitle {
    color: var(--muted);
    font-size: 1.05rem;
    line-height: 1.7;
    max-width: 500px;
  }

  /* ── LANGUAGES GRID ── */
  .languages-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
    gap: 1rem;
    margin-top: 3.5rem;
  }

  .lang-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 1.5rem 1rem;
    text-align: center;
    cursor: pointer;
    transition: transform 0.25s, border-color 0.25s, box-shadow 0.25s;
    position: relative;
    overflow: hidden;
  }

  .lang-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(240,192,64,0.05), transparent);
    opacity: 0;
    transition: opacity 0.25s;
  }

  .lang-card:hover {
    transform: translateY(-4px);
    border-color: rgba(240,192,64,0.4);
    box-shadow: 0 12px 30px rgba(0,0,0,0.3);
  }

  .lang-card:hover::before { opacity: 1; }

  .lang-flag { font-size: 2.5rem; margin-bottom: 0.7rem; display: block; }

  .lang-name {
    font-family: 'Syne', sans-serif;
    font-weight: 700;
    font-size: 0.95rem;
    margin-bottom: 0.3rem;
  }

  .lang-level {
    font-size: 0.75rem;
    color: var(--muted);
  }

  .lang-card.featured {
    border-color: rgba(240,192,64,0.5);
    background: linear-gradient(135deg, rgba(240,192,64,0.08), var(--surface));
  }

  .lang-badge {
    position: absolute;
    top: 0.7rem;
    right: 0.7rem;
    background: var(--accent);
    color: var(--bg);
    font-size: 0.6rem;
    font-weight: 700;
    padding: 0.2rem 0.5rem;
    border-radius: 100px;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  /* ── HOW IT WORKS ── */
  .steps {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    margin-top: 3.5rem;
  }

  .step-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 20px;
    padding: 2rem;
    position: relative;
    transition: transform 0.2s;
  }

  .step-card:hover { transform: translateY(-3px); }

  .step-number {
    font-family: 'Syne', sans-serif;
    font-size: 4rem;
    font-weight: 800;
    color: rgba(240,192,64,0.12);
    line-height: 1;
    margin-bottom: 1rem;
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
  }

  .step-icon {
    font-size: 2rem;
    margin-bottom: 1rem;
    display: block;
  }

  .step-title {
    font-family: 'Syne', sans-serif;
    font-size: 1.1rem;
    font-weight: 700;
    margin-bottom: 0.6rem;
  }

  .step-desc {
    color: var(--muted);
    font-size: 0.9rem;
    line-height: 1.6;
  }

  /* ── FEATURES ── */
  .features-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    margin-top: 3.5rem;
  }

  .feature-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 20px;
    padding: 2rem;
    transition: transform 0.2s, border-color 0.2s;
  }

  .feature-card:hover {
    transform: translateY(-3px);
    border-color: rgba(240,192,64,0.3);
  }

  .feature-card.wide {
    grid-column: span 2;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    align-items: center;
  }

  .feature-icon {
    font-size: 2rem;
    margin-bottom: 1rem;
    display: block;
  }

  .feature-title {
    font-family: 'Syne', sans-serif;
    font-size: 1.15rem;
    font-weight: 700;
    margin-bottom: 0.6rem;
  }

  .feature-desc {
    color: var(--muted);
    font-size: 0.9rem;
    line-height: 1.65;
  }

  .feature-visual {
    background: var(--surface2);
    border-radius: 12px;
    padding: 1.5rem;
    border: 1px solid var(--border);
  }

  .progress-item {
    margin-bottom: 1rem;
  }

  .progress-item:last-child { margin-bottom: 0; }

  .progress-label {
    display: flex;
    justify-content: space-between;
    font-size: 0.82rem;
    margin-bottom: 0.4rem;
    color: var(--muted);
  }

  .progress-bar {
    height: 6px;
    background: rgba(255,255,255,0.06);
    border-radius: 100px;
    overflow: hidden;
  }

  .progress-fill {
    height: 100%;
    border-radius: 100px;
    background: linear-gradient(90deg, var(--accent), var(--accent3));
    animation: fillBar 1.5s ease forwards;
    transform-origin: left;
  }

  @keyframes fillBar {
    from { width: 0 !important; }
  }

  /* ── TESTIMONIALS ── */
  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
    margin-top: 3.5rem;
  }

  .testimonial-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 20px;
    padding: 2rem;
  }

  .stars {
    color: var(--accent);
    font-size: 1rem;
    margin-bottom: 1rem;
    letter-spacing: 0.1em;
  }

  .testimonial-text {
    color: var(--text);
    font-size: 0.95rem;
    line-height: 1.7;
    margin-bottom: 1.5rem;
    font-style: italic;
  }

  .testimonial-author {
    display: flex;
    align-items: center;
    gap: 0.8rem;
  }

  .author-avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    background: var(--surface2);
  }

  .author-name {
    font-weight: 600;
    font-size: 0.9rem;
  }

  .author-meta {
    color: var(--muted);
    font-size: 0.78rem;
  }

  /* ── PRICING ── */
  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
    margin-top: 3.5rem;
    align-items: start;
  }

  .pricing-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 24px;
    padding: 2.5rem;
    transition: transform 0.2s;
  }

  .pricing-card:hover { transform: translateY(-4px); }

  .pricing-card.popular {
    border-color: var(--accent);
    background: linear-gradient(135deg, rgba(240,192,64,0.07), var(--surface));
    position: relative;
  }

  .popular-badge {
    position: absolute;
    top: -1px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--accent);
    color: var(--bg);
    font-size: 0.7rem;
    font-weight: 700;
    padding: 0.3rem 1rem;
    border-radius: 0 0 10px 10px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .plan-name {
    font-family: 'Syne', sans-serif;
    font-size: 1rem;
    font-weight: 700;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 1rem;
  }

  .plan-price {
    font-family: 'Syne', sans-serif;
    font-size: 3rem;
    font-weight: 800;
    line-height: 1;
    margin-bottom: 0.3rem;
  }

  .plan-price sup {
    font-size: 1.2rem;
    vertical-align: super;
    font-weight: 600;
  }

  .plan-period {
    color: var(--muted);
    font-size: 0.85rem;
    margin-bottom: 1.5rem;
  }

  .plan-features {
    list-style: none;
    margin-bottom: 2rem;
  }

  .plan-features li {
    padding: 0.5rem 0;
    font-size: 0.9rem;
    color: var(--muted);
    display: flex;
    align-items: center;
    gap: 0.6rem;
    border-bottom: 1px solid rgba(255,255,255,0.04);
  }

  .plan-features li:last-child { border-bottom: none; }

  .plan-features li::before {
    content: '✓';
    color: var(--accent3);
    font-weight: 700;
    font-size: 0.8rem;
    flex-shrink: 0;
  }

  .plan-features li.no::before { content: '×'; color: var(--muted); }
  .plan-features li.no { opacity: 0.5; }

  /* ── CTA ── */
  .cta-section {
    text-align: center;
    background: linear-gradient(135deg, rgba(240,192,64,0.06), rgba(78,205,196,0.04));
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }

  .cta-section h2 {
    font-family: 'Syne', sans-serif;
    font-size: clamp(2rem, 4vw, 3.5rem);
    font-weight: 800;
    letter-spacing: -0.03em;
    line-height: 1.1;
    margin-bottom: 1rem;
  }

  .cta-section p {
    color: var(--muted);
    font-size: 1.05rem;
    margin-bottom: 2.5rem;
  }

  /* ── FOOTER ── */
  footer {
    padding: 3rem 2rem;
    border-top: 1px solid var(--border);
  }

  .footer-inner {
    max-width: 1100px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .footer-logo {
    font-family: 'Syne', sans-serif;
    font-weight: 800;
    font-size: 1.2rem;
  }

  .footer-logo span { color: var(--accent); }

  .footer-links {
    display: flex;
    gap: 2rem;
    list-style: none;
  }

  .footer-links a {
    text-decoration: none;
    color: var(--muted);
    font-size: 0.85rem;
    transition: color 0.2s;
  }

  .footer-links a:hover { color: var(--text); }

  .footer-copy {
    color: var(--muted);
    font-size: 0.8rem;
  }

  /* Scroll reveal */
  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.7s ease, transform 0.7s ease;
  }
  .reveal.visible {
    opacity: 1;
    transform: none;
  }

  @media (max-width: 768px) {
    nav { padding: 1rem 1.5rem; }
    .nav-links { display: none; }
    .stat-item { padding: 1.5rem 2rem; }
    .features-layout { grid-template-columns: 1fr; }
    .feature-card.wide { grid-column: span 1; grid-template-columns: 1fr; }
    .footer-inner { flex-direction: column; text-align: center; }
  }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <div class="logo">UYL<span>.</span></div>
  <ul class="nav-links">
    <li><a href="#languages">Languages</a></li>
    <li><a href="#how">How it Works</a></li>
    <li><a href="#features">Features</a></li>
    <li><a href="#pricing">Pricing</a></li>
    <li><a href="#pricing" class="nav-cta">Start Free</a></li>
  </ul>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="lang-bubbles">
    <span class="bubble">你好</span>
    <span class="bubble">Bonjour</span>
    <span class="bubble">مرحبا</span>
    <span class="bubble">こんにちは</span>
    <span class="bubble">Hola</span>
    <span class="bubble">Ciao</span>
    <span class="bubble">Olá</span>
    <span class="bubble">Привет</span>
  </div>
  <div class="hero-glow"></div>
  <div class="hero-glow2"></div>

  <div class="hero-badge">🌍 20+ Languages Available</div>

  <h1>
    Speak the world's<br>
    <span class="line2">languages</span>
  </h1>

  <p>UYL — Up Your Language — turns language learning into an obsession, not a chore. Bite-sized lessons, real conversations, unstoppable progress.</p>

  <div class="hero-buttons">
    <button class="btn-primary" onclick="document.getElementById('languages').scrollIntoView({behavior:'smooth'})">Start Learning Free</button>
    <button class="btn-secondary" onclick="document.getElementById('how').scrollIntoView({behavior:'smooth'})">See How It Works</button>
  </div>
</section>

<!-- STATS -->
<div class="stats">
  <div class="stat-item reveal">
    <div class="stat-number">2.4M</div>
    <div class="stat-label">Active Learners</div>
  </div>
  <div class="stat-item reveal">
    <div class="stat-number">20+</div>
    <div class="stat-label">Languages</div>
  </div>
  <div class="stat-item reveal">
    <div class="stat-number">94%</div>
    <div class="stat-label">Completion Rate</div>
  </div>
  <div class="stat-item reveal">
    <div class="stat-number">4.9★</div>
    <div class="stat-label">App Rating</div>
  </div>
</div>

<!-- LANGUAGES -->
<section id="languages">
  <div class="container">
    <div class="section-tag">Languages</div>
    <h2 class="section-title">Pick your language.<br>We'll do the rest.</h2>
    <p class="section-subtitle">From the world's most spoken languages to hidden gems — every course is crafted by native speakers and learning scientists.</p>

    <div class="languages-grid reveal">
      <div class="lang-card featured">
        <span class="lang-badge">Popular</span>
        <span class="lang-flag">🇪🇸</span>
        <div class="lang-name">Spanish</div>
        <div class="lang-level">500M+ speakers</div>
      </div>
      <div class="lang-card featured">
        <span class="lang-badge">Hot</span>
        <span class="lang-flag">🇨🇳</span>
        <div class="lang-name">Mandarin</div>
        <div class="lang-level">1B+ speakers</div>
      </div>
      <div class="lang-card">
        <span class="lang-flag">🇫🇷</span>
        <div class="lang-name">French</div>
        <div class="lang-level">280M speakers</div>
      </div>
      <div class="lang-card">
        <span class="lang-flag">🇩🇪</span>
        <div class="lang-name">German</div>
        <div class="lang-level">100M speakers</div>
      </div>
      <div class="lang-card">
        <span class="lang-flag">🇯🇵</span>
        <div class="lang-name">Japanese</div>
        <div class="lang-level">125M speakers</div>
      </div>
      <div class="lang-card">
        <span class="lang-flag">🇧🇷</span>
        <div class="lang-name">Portuguese</div>
        <div class="lang-level">250M speakers</div>
      </div>
      <div class="lang-card">
        <span class="lang-flag">🇮🇹</span>
        <div class="lang-name">Italian</div>
        <div class="lang-level">65M speakers</div>
      </div>
      <div class="lang-card">
        <span class="lang-flag">🇸🇦</span>
        <div class="lang-name">Arabic</div>
        <div class="lang-level">420M speakers</div>
      </div>
      <div class="lang-card">
        <span class="lang-flag">🇰🇷</span>
        <div class="lang-name">Korean</div>
        <div class="lang-level">77M speakers</div>
      </div>
      <div class="lang-card">
        <span class="lang-flag">🇷🇺</span>
        <div class="lang-name">Russian</div>
        <div class="lang-level">150M speakers</div>
      </div>
      <div class="lang-card">
        <span class="lang-flag">🇳🇱</span>
        <div class="lang-name">Dutch</div>
        <div class="lang-level">24M speakers</div>
      </div>
      <div class="lang-card">
        <span class="lang-flag">🇹🇷</span>
        <div class="lang-name">Turkish</div>
        <div class="lang-level">80M speakers</div>
      </div>
    </div>
  </div>
</section>

<!-- HOW IT WORKS -->
<section id="how" style="background: var(--surface);">
  <div class="container">
    <div class="section-tag">Process</div>
    <h2 class="section-title">Fluency in 4 steps</h2>
    <p class="section-subtitle">Our method is built on science — spaced repetition, immersive recall, and real human connection.</p>

    <div class="steps">
      <div class="step-card reveal">
        <div class="step-number">01</div>
        <span class="step-icon">🎯</span>
        <div class="step-title">Set Your Goal</div>
        <p class="step-desc">Tell us why you're learning — travel, work, romance — and we build a personalized path just for you.</p>
      </div>
      <div class="step-card reveal">
        <div class="step-number">02</div>
        <span class="step-icon">🧠</span>
        <div class="step-title">Daily Micro-Lessons</div>
        <p class="step-desc">10–15 minutes a day. Our AI adapts to your pace, repeating what you forget and accelerating what you know.</p>
      </div>
      <div class="step-card reveal">
        <div class="step-number">03</div>
        <span class="step-icon">🗣️</span>
        <div class="step-title">Speak With Natives</div>
        <p class="step-desc">Practice live with native speakers in our community. No judgment, only growth.</p>
      </div>
      <div class="step-card reveal">
        <div class="step-number">04</div>
        <span class="step-icon">🏆</span>
        <div class="step-title">Track & Celebrate</div>
        <p class="step-desc">Streaks, milestones, certificates. See exactly how far you've come and how close you are to fluency.</p>
      </div>
    </div>
  </div>
</section>

<!-- FEATURES -->
<section id="features">
  <div class="container">
    <div class="section-tag">Features</div>
    <h2 class="section-title">Everything you need.<br>Nothing you don't.</h2>

    <div class="features-layout">
      <div class="feature-card wide reveal">
        <div>
          <span class="feature-icon">📈</span>
          <div class="feature-title">Smart Progress Tracking</div>
          <p class="feature-desc">Watch your vocabulary, grammar, and speaking skills grow in real time. Our dashboard breaks down exactly where you're strong and where to push harder.</p>
        </div>
        <div class="feature-visual">
          <div class="progress-item">
            <div class="progress-label"><span>Vocabulary</span><span>78%</span></div>
            <div class="progress-bar"><div class="progress-fill" style="width:78%"></div></div>
          </div>
          <div class="progress-item">
            <div class="progress-label"><span>Grammar</span><span>61%</span></div>
            <div class="progress-bar"><div class="progress-fill" style="width:61%"></div></div>
          </div>
          <div class="progress-item">
            <div class="progress-label"><span>Speaking</span><span>45%</span></div>
            <div class="progress-bar"><div class="progress-fill" style="width:45%"></div></div>
          </div>
          <div class="progress-item">
            <div class="progress-label"><span>Listening</span><span>83%</span></div>
            <div class="progress-bar"><div class="progress-fill" style="width:83%"></div></div>
          </div>
        </div>
      </div>

      <div class="feature-card reveal">
        <span class="feature-icon">🤖</span>
        <div class="feature-title">AI Conversation Partner</div>
        <p class="feature-desc">Practice speaking 24/7 with our AI tutor that corrects your pronunciation, suggests better phrases, and keeps the conversation going.</p>
      </div>

      <div class="feature-card reveal">
        <span class="feature-icon">🔥</span>
        <div class="feature-title">Streak System</div>
        <p class="feature-desc">Daily streaks keep you accountable. Miss a day? Streak shields have you covered. Consistency is the real secret to fluency.</p>
      </div>

      <div class="feature-card reveal">
        <span class="feature-icon">🎧</span>
        <div class="feature-title">Immersive Audio</div>
        <p class="feature-desc">Real native speakers. Real accents. Real context. Our audio library includes podcasts, stories, and conversations for every level.</p>
      </div>

      <div class="feature-card reveal">
        <span class="feature-icon">👥</span>
        <div class="feature-title">Global Community</div>
        <p class="feature-desc">Join language clubs, compete in weekly challenges, and connect with millions of learners across the world.</p>
      </div>
    </div>
  </div>
</section>

<!-- TESTIMONIALS -->
<section style="background: var(--surface);">
  <div class="container">
    <div class="section-tag">Testimonials</div>
    <h2 class="section-title">Learners love UYL</h2>

    <div class="testimonials-grid">
      <div class="testimonial-card reveal">
        <div class="stars">★★★★★</div>
        <p class="testimonial-text">"I went from zero to holding a full conversation in Spanish in just 4 months. The AI tutor is like having a patient teacher available at 2am."</p>
        <div class="testimonial-author">
          <div class="author-avatar">👩🏽</div>
          <div>
            <div class="author-name">Aisha K.</div>
            <div class="author-meta">Learning Spanish · Lagos, Nigeria</div>
          </div>
        </div>
      </div>
      <div class="testimonial-card reveal">
        <div class="stars">★★★★★</div>
        <p class="testimonial-text">"I tried every app out there. UYL is the only one that actually made me stick. The streak system is addictive in the best way."</p>
        <div class="testimonial-author">
          <div class="author-avatar">👨🏻</div>
          <div>
            <div class="author-name">Marcus T.</div>
            <div class="author-meta">Learning Japanese · Berlin, Germany</div>
          </div>
        </div>
      </div>
      <div class="testimonial-card reveal">
        <div class="stars">★★★★★</div>
        <p class="testimonial-text">"Within 3 months I was reading Mandarin characters I never thought I could learn. The bite-sized format just works. Period."</p>
        <div class="testimonial-author">
          <div class="author-avatar">👩🏼</div>
          <div>
            <div class="author-name">Sofia R.</div>
            <div class="author-meta">Learning Mandarin · São Paulo, Brazil</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- PRICING -->
<section id="pricing">
  <div class="container">
    <div class="section-tag">Pricing</div>
    <h2 class="section-title">Invest in yourself.</h2>
    <p class="section-subtitle">Start free, upgrade when you're ready. No pressure, no hidden fees.</p>

    <div class="pricing-grid">
      <div class="pricing-card reveal">
        <div class="plan-name">Free</div>
        <div class="plan-price"><sup>$</sup>0</div>
        <div class="plan-period">forever</div>
        <ul class="plan-features">
          <li>1 language</li>
          <li>10 lessons/month</li>
          <li>Basic progress tracker</li>
          <li class="no">AI conversation partner</li>
          <li class="no">Native speaker sessions</li>
          <li class="no">Offline mode</li>
        </ul>
        <button class="btn-secondary" style="width:100%; padding:0.85rem;">Get Started</button>
      </div>

      <div class="pricing-card popular reveal">
        <div class="popular-badge">Most Popular</div>
        <div class="plan-name">Pro</div>
        <div class="plan-price"><sup>$</sup>12</div>
        <div class="plan-period">per month · billed annually</div>
        <ul class="plan-features">
          <li>5 languages</li>
          <li>Unlimited lessons</li>
          <li>Full progress dashboard</li>
          <li>AI conversation partner</li>
          <li>2 native speaker sessions/mo</li>
          <li>Offline mode</li>
        </ul>
        <button class="btn-primary" style="width:100%; padding:0.85rem;">Start Pro Free</button>
      </div>

      <div class="pricing-card reveal">
        <div class="plan-name">Elite</div>
        <div class="plan-price"><sup>$</sup>29</div>
        <div class="plan-period">per month · billed annually</div>
        <ul class="plan-features">
          <li>All 20+ languages</li>
          <li>Unlimited everything</li>
          <li>Advanced analytics</li>
          <li>Unlimited AI sessions</li>
          <li>Unlimited native sessions</li>
          <li>Certification program</li>
        </ul>
        <button class="btn-secondary" style="width:100%; padding:0.85rem;">Go Elite</button>
      </div>
    </div>
  </div>
</section>

<!-- CTA -->
<section class="cta-section">
  <div class="container">
    <h2>Your fluency journey<br>starts today.</h2>
    <p>Join 2.4 million learners already speaking a new language with UYL.</p>
    <button class="btn-primary" style="font-size:1.1rem; padding:1rem 2.8rem;">Start For Free →</button>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-inner">
    <div class="footer-logo">UYL<span>.</span></div>
    <ul class="footer-links">
      <li><a href="#">About</a></li>
      <li><a href="#">Blog</a></li>
      <li><a href="#">Careers</a></li>
      <li><a href="#">Privacy</a></li>
      <li><a href="#">Terms</a></li>
    </ul>
    <p class="footer-copy">© 2026 Up Your Language. All rights reserved.</p>
  </div>
</footer>

<script>
  // Scroll reveal
  const reveals = document.querySelectorAll('.reveal');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.classList.add('visible');
        observer.unobserve(e.target);
      }
    });
  }, { threshold: 0.1 });
  reveals.forEach(el => observer.observe(el));

  // Stagger children
  document.querySelectorAll('.languages-grid, .steps, .features-layout, .testimonials-grid, .pricing-grid').forEach(grid => {
    [...grid.children].forEach((child, i) => {
      child.style.transitionDelay = `${i * 0.07}s`;
    });
  });
</script>
</body>
</html>
